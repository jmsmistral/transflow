# Web source boundary

T006 will establish the TypeScript/React application after T003 qualifies its
dependencies. The [canonical UI specification](../../transflow-spec/TECHNICAL_ARCHITECTURE.md#16-lineage-ui-specification)
defines the delivered behaviour to implement.

No frontend build exists yet. Private screenshot references stay in the ignored
specification reference directory and must not become application assets.
